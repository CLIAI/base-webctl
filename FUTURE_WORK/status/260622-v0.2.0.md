# Status — base-webctl v0.2.0 (2026-06-22)

**Result: GREEN.** Second small-refactoring step done. 5 clean leaf modules
extracted, consumption re-proven in scratch with all 6 base modules
(docker-ctl + the 5) shimmed.

## What landed in base (committed on `master`; pushed/tagged pending)

* `lib/browser-location/cdp-rewrite.js`, `lib/browser-location/localhost-direct.js`,
  `lib/xpra-presence.js`, `lib/lru-cleanup.js`, `lib/systemd-timer.js` — verbatim
  CJS→ESM ports + JSDoc, all re-exported via `lib/index.js`.
* `package.json` bumped to **0.2.0**.
* `test/*.test.js` for each → **38/38** `node --test`; **`tsc --checkJs` clean**.

## Consumption proof (scratch — consumers NOT touched)

* linkedin-scratch@`8e85d31`, submodule pinned at base `9d9cfbd`, all 6 modules
  shimmed. **24/24 `test/*-test.js` green.** Witnesses through the submodule:
  lru-cleanup 112, browser-location 23, systemd-timer 10, xpra-presence 5,
  profile-lock 18, docker-mode 57. **+20 / -759** lines across the 5 shims.
* Patch: `/tmp/claude/260622-1545-webctl-base-pilot/linkedin-v0.2.0-five-modules.patch`.

## Proposals delivered (PR-ready, not pushed)

* `proposals/2026-06-22-linkedin-v0.2.0-five-modules.md` (proven against linkedin).
* `proposals/2026-06-22-chatgpt-v0.2.0-five-modules.md` (byte-identical; chatgpt
  runs its own suite before bumping — test-before-bump).

## Scope split (the "10 modules" was not uniform)

Of the 10 the manager listed, only **5 were dependency-free leaves** and shipped.
The other 5 are blocked on design/deps, NOT effort:

* **SEAM-bound (3):** `chromium-prefs`, `client-config`, `profile-lock` — load
  `client-config.constants` (per-repo seam). → `FUTURE_WORK/migrate/260622-client-config-constants-seam.md`
* **Sibling-dep (2):** `browser-location/{registry,index}` — need the
  `chromium-docker-xpra` driver + `mounts`. → `FUTURE_WORK/migrate/260622-registry-index-driver-deps.md`
* **NEW drift finding:** `mounts.js` has **already diverged** between linkedin
  and chatgpt (`cmp` fails) AND binds the seam — needs a reconcile + the seam
  design before extraction. (A live example of the byte-dup drift sb7q warns of.)

## Critical-path recommendation for the manager

The **constants-seam design** gates the 3 SEAM modules, `mounts`, and the driver
— i.e. nearly all remaining migration. Highest-leverage next decision. `sb7q`
already leans toward a "resolver + constants template" mechanism; it needs a
short design doc nailing the resolution order before more code.

## Base release state

* **DONE:** `master` pushed + tag **v0.2.0** (`5032279`) on
  `git@github.com:CLIAI/base-webctl.git`. Release gate green (4× SKIP, exit 0)
  before tagging. v0.1.0 (`3cbfe63`) also pushed + tagged. Consumers can pin
  either tag now.
