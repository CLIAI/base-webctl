# v0.4.0 execution plan — READ THIS FIRST on resume (2026-06-23)

Post-/clear anchor. Greg approved the path; this is the concrete checklist.
Context: v0.3.0 shipped the sm2t seam (`createX(C)` factories). v0.4.0 continues
the sm2t migration order: **mounts → driver → registry → browser-location/index**.

## Ground rules (unchanged)

* Secret-free; zero runtime deps; ESM + JSDoc; **NO top-level await** (keep sync
  `require(esm)`); Node ≥22.12 floor; `tsc --checkJs` clean; small commits.
* **Do NOT touch live consumer repos.** Scratch-prove + per-module proposals +
  test-before-bump. Manager = `webctl:mgr` at `tmux -L default -t webctl-mgr:0`
  (introduce as `(webctl:base)`; collision-safe send; verify pane idle, not a menu).
* Seam pattern: consumer shim `module.exports = createX(require('./client-config.constants'))`.

## Step 1 — mounts (Greg-approved Option A: decouple at base)

* `mounts.js` is byte-DIVERGED linkedin↔chatgpt; **canonical = chatgpt** (it has
  the WAY-1 attach-upload mount). FULL finding:
  `FUTURE_WORK/migrate/260623-mounts-reconcile-file-staging-finding.md`.
* **createMounts(C) must stay file-staging-AGNOSTIC.** Port chatgpt's mounts.js
  body, but:
  * `CONTAINER_UPLOAD_DIR = '/cliai-uploads'` is **base-owned** (fixed cross-tool
    container path — verify exact value in chatgpt `lib/browser-location/file-staging.js`).
    Do NOT `require('./file-staging')` in base.
  * the upload mount is gated on `cfg.uploadHostPath` (added only when set).
* `mounts.js` uses `C.ARTIFACT_PREFIX`, `C.IMAGE_CHROMIUM_REPO`, `C.IMAGE_XPRA`,
  `C.CACHE_DIRNAME` — all function-scope → clean factory. Add `createMounts(C)`
  to `lib/index.js`. Unit-test + tsc.
* **Prove against BOTH consumers** in scratch: chatgpt sets `uploadHostPath` →
  upload mount appears; linkedin never sets it → behaviour unchanged. Run each
  repo's mounts/browser-location suites green via the submodule.
* Per-consumer proposals (linkedin + chatgpt), test-before-bump. Tag v0.4.0
  (or stage mounts in v0.4.0 with the driver — your call on granularity).

## Step 1b — QUEUE (do not implement now)

* Write `proposals/2026-06-23-linkedin-uploads-feature.md` (LOW-PRIORITY) +
  `FUTURE_WORK/migrate/260623-linkedin-uploads-feature-queue.md`: porting
  `file-staging.js` + its host-side callers + tests so linkedin GAINS the upload
  feature. Greg: **queue, no commitment** — linkedin-owner + Greg schedule later.
  Keep it OUT of the mounts extraction.

## Step 2 — chromium-docker-xpra driver

* Largest module. `createChromiumDockerXpra(C, deps)` where `deps` optionally
  injects `docker-ctl` / `mounts` for tests — **mirrors the profile-lock
  `dockerInspect` injection contract** (Greg-confirmed). Consumes docker-ctl ✓ +
  mounts (post-extract) + cdp-rewrite ✓ + injected C.
* Read linkedin `lib/browser-location/chromium-docker-xpra.js` fully first; check
  linkedin↔chatgpt byte-identity + any drift before porting.

## Step 3 — registry → Step 4 — browser-location/index

* `registry.js`: requires `./localhost-direct` (in base ✓) + `./chromium-docker-xpra`
  (after step 2). Factory if it needs C/driver; else may be near-direct.
* `browser-location/index.js`: top orchestrator; requires registry + mounts +
  `../client-config.constants` (seam). Factory `createBrowserLocation(C, deps)`. LAST.

## Releases so far

v0.1.0 `3cbfe63` · v0.2.0 `5032279` · v0.3.0 `4cc60d1`. Next: v0.4.0.

## On resume

1. Read this + `260623-v0.4.0-plan.md` (this file) + the finding doc + sm2t (stable).
2. `cd /home/gw-t490/github/CLIAI/base-webctl`; confirm `node --test` (55) + `tsc` clean.
3. Start Step 1 (mounts). Scratch clone path recorded in
   `/tmp/claude/.webctl-pilot-scratch-path` (re-clone if /tmp was cleared).
4. Ping `webctl:mgr` at the v0.4.0 milestone (or sooner if something surfaces).
