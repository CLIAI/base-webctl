# Status — base-webctl v0.3.0 (2026-06-23)

**Result: GREEN.** The Greg-approved constants-injection seam (sm2t, Option A)
is implemented and the 3 config-bound modules are factory-extracted + proven.

## What landed in base (on origin; tagged v0.3.0)

* **Seam support:** `lib/client-config.constants.template.js` —
  `ClientConfigConstants` typedef + `assertConstants` (default-on, fail-fast) +
  a SHAPE-ONLY `TEMPLATE` whose every access throws.
* **3 factories** (verbatim bodies, `C` injected, module-scope reads moved into
  the closure): `createClientConfig(C)`, `createChromiumPrefs(C)`,
  `createProfileLock(C)`. Shared-identical constants (port offsets,
  `ENV_SUFFIXES`, …) are now **base-owned**, not in the seam.
* All re-exported via `lib/index.js`. **55/55 `node --test`**, **`tsc --checkJs`
  clean**. No top-level await (sync `require(esm)` preserved).
* `sm2t` flipped **draft → stable** (Greg-approved); open questions recorded
  resolved (throwing template; assert default-on; driver-deps mirror profile-lock).

## Consumption proof (scratch — consumers NOT touched)

* linkedin-scratch@`8e85d31`, submodule pinned at v0.3.0 (`4cc60d1`), 3 shims
  injecting the consumer's own `client-config.constants`:
  * `client-config-test` **79**, `chromium-prefs-test` **68**,
    `profile-lock-test` **18** — all green **through the submodule**.
  * Full suite: **24/24** test files green, **9** modules now from base, zero
    collateral breakage. **+10 / -1241** lines across the 3 shims.
* Patch: `/tmp/claude/260622-1545-webctl-base-pilot/linkedin-v0.3.0-seam-modules.patch`.

## Proposals delivered (PR-ready, NOT pushed to consumers)

* `proposals/2026-06-23-linkedin-v0.3.0-seam-modules.md` (proven).
* `proposals/2026-06-23-chatgpt-v0.3.0-seam-modules.md` (byte-identical;
  test-before-bump).

## Invariant checks

* **Seam holds:** no real per-repo VALUES in base/lib (verified — only illustrative
  comment examples remain; logged
  `FUTURE_WORK/reconcile/260623-service-agnostic-naming-scrub.md` for a later
  cosmetic + `linkedInTabs`-rename pass, which is API-coordinated, non-blocking).
* Secret-free, zero runtime deps, ESM+JSDoc all hold.

## Releases on origin

* v0.1.0 `3cbfe63` · v0.2.0 `5032279` · **v0.3.0 `4cc60d1`**.

## PARKED for the next step (sm2t migration order)

* `mounts.js` — drifted linkedin↔chatgpt; **reconcile-to-canonical first**
  (manager: canonical = chatgpt / the WAY-1 upload fix), then factory-extract.
* `chromium-docker-xpra` driver (`createChromiumDockerXpra(C, deps)`; deps mirror
  the profile-lock injection), then `registry`, then browser-location `index`.
* Refs: `FUTURE_WORK/migrate/260622-registry-index-driver-deps.md`.
