# Status — base-webctl v0.5.0 (2026-06-23)

**Result: GREEN.** v0.5.0 = the **driver chain** step of the sm2t migration:
`browser-location/` is now FULLY extracted to base (driver + registry + xpra-attach
+ index aggregator), proven against BOTH consumers. Greenlit by `webctl:mgr` with
a pre-work drift-map brief; no Greg decision needed (all within approved Option A).

## What landed in base (tagged v0.5.0)

* **`chromium-docker-xpra.js`** → `createChromiumDockerXpra(C, opts)`. Ported from
  chatgpt's canonical driver (strict superset of linkedin, +19/−0, all WAY-1 upload
  staging). **File-staging-AGNOSTIC** like createMounts: upload staging wired via
  OPTIONAL `opts.fileStaging` ({resolveUploadDirs, ensureUploadHostDir,
  UPLOAD_STAGE_ENV}); absent → byte-unchanged (linkedin). Sibling base surfaces
  (docker-ctl + mounts/profile-lock/chromium-prefs factories) default from C,
  overridable via opts (profile-lock dockerInspect contract). **`opts.version`**
  records the tool version in the lock — was a repo-relative
  `require('../../package.json')`, a 2nd instance of the dockerfilesDir-class bug.
* **`registry.js`** → `createRegistry(C, opts)`. Content byte-identical across
  consumers; a factory only because it builds the C-bound driver + references the
  constants-free localhost-direct module. Forwards driver opts.
* **`xpra-attach.js`** → `createXpraAttach(C)`. The 5th drifted module the original
  plan didn't name (manager's drift-map). **linkedin-canonical** (parameterizes
  `opts.html5Port`, default port+1; chatgpt hardcoded 14501) — chatgpt gains a
  behaviour-identical-at-default improvement.
* **`browser-location/index.js`** → `createBrowserLocation(C, opts)`. The chain
  AGGREGATOR, extracted LAST: builds the C-bound registry (→ driver) + references
  mounts.migrationBannerMarker; driver opts thread through. `opts.fs` test seam.
* All wired via `lib/index.js`. **101/101 `node --test`** (+32 over v0.4.0),
  **`tsc --checkJs` clean**. No top-level await.

## Consumption proof (scratch worktrees — consumers NOT touched, removed after)

5 shims per consumer (mounts/driver/registry/xpra-attach/index) onto the base
factories; chatgpt injects `fileStaging`, linkedin omits it.

* **chatgpt** @ HEAD: `tests/docker-mode-test.js` **59/0**; full `tests/unit/*`
  **864/0**. Driver inspect() upload gate ON (`uploadContainerDir=/cliai-uploads`).
* **linkedin** @ HEAD: `test/docker-mode-test.js` **57/0**;
  `test/browser-location-test.js` **23/0** (the only chain-touching suites). Driver
  inspect() upload gate OFF → byte-unchanged.
* Both suites exercise index → registry → driver(factoryForBase) → mounts +
  xpra-attach, so all five seams are proven through real downstream callers.
* Evidence: `/tmp/claude/260623-chain-proof/PROOF.md`.

## Proposals delivered (PR-ready, NOT pushed to consumers)

* `proposals/2026-06-23-linkedin-v0.5.0-browser-location-chain.md` (5 shims, no
  fileStaging, xpra-attach is a no-op for linkedin = canonical side).
* `proposals/2026-06-23-chatgpt-v0.5.0-browser-location-chain.md` (5 shims, injects
  fileStaging; chatgpt's FIRST base adoption — pins the submodule).

## Discoveries (plan didn't flag)

* **`opts.version`** — 2nd repo-relative bug after dockerfilesDir (the lock's tool
  version). Same injection fix. Note:
  `FUTURE_WORK/migrate/260623-dockerfiles-dir-injection-seam.md`.
* **xpra-attach.js** — 5th shared browser-location module (manager-surfaced), now
  folded in so `browser-location/` is genuinely complete, not falsely done at index.

## Releases on origin

* v0.1.0 `3cbfe63` · v0.2.0 `5032279` · v0.3.0 `7634f67` · v0.4.0 `72f2e9c` ·
  **v0.5.0 (this)**.

## PARKED / next

* `browser-location/` is DONE on base. Remaining cross-consumer shared surface is
  confined to it (manager's full-`lib/` scan: nothing shared hides outside
  browser-location/). Per-repo `client-config.constants.js` is intentionally
  DRIFTED (the seam) — never an extraction candidate.
* Consumers adopt v0.4.0 mounts + v0.5.0 chain at their own pace (test-before-bump).
  chatgpt has adopted ZERO base modules so far (mid its own parser track).
* linkedin uploads FEATURE stays queued (low-pri, no commitment).
