# Status — base-webctl pilot vertical slice (2026-06-22)

**Result: GREEN.** The whole pipeline — lib module → ESM export → git-submodule
consumption → the test gate — is proven on one small, safe module
(`docker-ctl`), end to end.

## What landed in base (committed on `master`)

* `lib/browser-location/docker-ctl.js` — linkedin's docker-ctl ported verbatim
  to zero-dep ESM + JSDoc (xq MIT header preserved; `^name$` multi-tenant
  anchoring intact). `lib/index.js` public surface.
* `package.json` (`type:module`, `@cliai/base-webctl@0.1.0`, exports map,
  typescript + @types/node **devDeps only**) + `tsconfig.json` (checkJs/noEmit).
* `test/docker-ctl.test.js` — **14/14** `node --test` pass; `tsc --checkJs` clean.
* Gate scaffolding (xrl4): `consumers.jsonc` (4 consumers seeded,
  `"wired": false`), `scripts/test-all-consumers.sh`,
  `scripts/verify-no-byte-drift.sh`, `scripts/read-consumers.mjs` — each gate
  script with `.README.md` + `.DEV_NOTES.md`. Gate run today: 4× SKIP, exit 0
  (graceful no-op until consumers wire in).

## Consumption proof (scratch — linkedin NOT touched)

* Throwaway clone of linkedin-webctl@`8e85d31` at
  `/tmp/claude/260622-1545-webctl-base-pilot/linkedin-scratch`.
* Added base as submodule `vendor/base-webctl`, replaced docker-ctl.js with a
  15-line re-export shim (`+14 / -297`).
* `node test/docker-mode-test.js`: **57 passed** before == **57 passed** after.
  All **24** `test/*-test.js` green — zero collateral breakage. docker-ctl
  mocked-spawn tests pass **through the submodule** (monkey-patch seam survives
  via `require(esm)` + call-time `child_process.spawn`).
* Proposal for the linkedin owner: `proposals/2026-06-22-linkedin-docker-ctl-submodule.md`.
  Patch archived at `…/linkedin-scratch` → `linkedin-docker-ctl-submodule.patch`.

## Open follow-ons (deferred, non-blocking)

* **DONE (2026-06-22):** base pushed + tagged `v0.1.0` (`3cbfe63`) on origin;
  consumers can pin that SHA. (Superseded by `v0.2.0` — see the v0.2.0 status.)
* `FUTURE_WORK/migrate/260622-linkedin-node-floor-require-esm.md` — shim needs
  Node ≥22.12; linkedin engine-floor decision is the consumer's.
* `FUTURE_WORK/test/260622-fresh-clone-ci-gate.md` — hermetic fresh-clone gate
  mode (needs deploy keys + per-consumer `test-against-base.sh`).

## Next small refactoring

Await manager's pick. Natural candidates: replicate the proven shim pattern for
chatgpt-webctl's docker-ctl (byte-identical today), or extract the next
self-contained module (e.g. `cdp-rewrite` / `mounts`).
